# Rename this file to .env and fill values.
PORT=5173
HOST=0.0.0.0

# Where to store SQLite DB
DB_PATH=./wallet.db

# Public URL where Telegram WebApp is served (include protocol)
WEBAPP_URL=http://localhost:5173/webapp/

# Telegram Bot token, get from @BotFather
TELEGRAM_BOT_TOKEN=REPLACE_ME

# Set to 1 to use mock auth response instead of Polygon ID js-sdk
ENABLE_MOCKS=1

# === JS SDK / proofs ===
# путь к распакованным схемам (после `npm run circuits`)
CIRCUITS_PATH=./sdk/circuits

# сеть/версификатор состояния (минимум RPC_URL для state checks)
# Для базовой Auth (без query-based проверок) можно оставить пустым,
# но лучше задать Amoy RPC:
# RPC_URL=https://polygon-amoy.g.alchemy.com/v2/REPLACE_ME
# STATE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
# RHS_URL=https://rhs.privado.id